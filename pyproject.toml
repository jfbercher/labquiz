
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "labquiz"
version = "0.7.7"
description = "Quizzes intégrés pour Jupyter"
license = "GPL-3.0-or-later"
authors = [{ name="J.-F. Bercher", email="jf.bercher@esiee.fr" }]
dependencies = [
    "ipywidgets>=7.6.5",
    "pyyaml",
    "cryptography",
    "ipython",
    "numpy",
    "pandas",
    "requests",
] 
keywords = ["quizzes", "jupyter", "hash:f3a0875d6d7dbeea00dbe323e400a51f77339a653958b197c3113f1b81d2fe7c"]
